@using Microsoft.AspNetCore.Mvc.Localization
@model HomeViewModel

@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = "Home Page";
}
<div id="content">
    <div class="col-sm-2">
        <ul id="Category" class="nav nav-tabs nav-pills nav-stacked well">
            @foreach (var item in Model.Categorys)
            {
                <li><a href="#vtab1" data-toggle="tab">@Localizer[item.Name]</a></li>
            }
        </ul>
        @*<div class="row">
                <div class="TagCloud">

                </div>
            </div>*@
    </div>
    <div class="col-sm-8">
        @*<table class="table table-bordered table-striped table-condensed">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>CategoryId</th>
                        <th>CategoryName</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Fanfics)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.Id)</td>
                            <td>@Html.DisplayFor(modelItem => item.Name)</td>
                            <td>@Html.DisplayFor(modelItem => item.Category.Id)</td>
                            <td>@Html.DisplayFor(modelItem => item.Category.Name)</td>
                        </tr>
                    }
                </tbody>
            </table>*@
        @foreach (var item in Model.Fanfics)
        {
        <div class="thumbnail">
            <div class="autherBlock">
                <div class="col-xs-6">
                    <div class="col-xs-2">
                        <img class="avatarImg" src="@item.ApplicationUser.AvatarUrl" style="height:37px;">
                    </div>
                    <div class="col-xs-10">
                        <div class="col-xs-12">
                           @item.ApplicationUser.UserName
                        </div>
                        <div class="col-xs-12">
                            @item.CreateDate.ToShortDateString()
                        </div>
                    </div>
                </div>
                <div class="col-xs-2 col-xs-offset-4">
                    @item.AverageRating
                </div>
            </div>
            <div class="imgFanfic"><img src="@item.ImgUrl"></div>
            <div class="caption">
                <p>@item.Description</p>
                <p><a href="#" class="btn btn-primary" role="button">@item.Category.Name</a> <a href="#" class="btn btn-default" role="button">Button</a></p>
            </div>
        </div>
        }
    </div>
</div>